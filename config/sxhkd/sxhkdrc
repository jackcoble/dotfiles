# ==============
# BSPWM Bindings
# ==============

# Quit BSPWM
super + shift + Escape
	bspc quit

# Restart BSPWM
super + shift + w
	bspc wm -r

# Kill Window/Pane
super + shift + q
	xdo close

# Change Window Focus
super + {h,j,k,l}
	bspc node -f {west,south,north,east}

# Send Window to different Desktop
super + shift + {0-9}
	bspc node -d {0-9}

# Send Window to Desktop and swap to Desktop
super + control + {0-9}
	bspc node -d {0-9} && bspc desktop -f {0-9}

# Swap Window with Direction
super + shift + {h,j,k,l}
	bspc node -s {west,south,north,east}

# Focus on Desktop
super + {0-9}
	bspc desktop -f {0-9}

# Cycle through Desktops
super + alt + {h, l}
	bspc desktop -f {prev, next}

# Fullscreen
super + f
	bspc node -t \~fullscreen

# ==============
# Programs
# ==============

# Terminal
super + Return
	$TERMINAL

# Rofi
super + space
	rofi -show run -config $HOME/.config/rofi/rofi.conf

# Stream script
super + shift + s
	$TERMINAL -e stream